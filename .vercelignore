# Usual ignores
*.csv
cookies.json
.env
__pycache__/
*.log
failed_*.json
progress_*.json

# Prevent public folder from interfering with vercel.json

# Exclude root index.html from static file serving so the / rewrite hits
# api/ssr/home.js instead (the function bundles it via includeFiles)
/index.html
public/
